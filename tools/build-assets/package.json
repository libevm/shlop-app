{
  "name": "@maple/build-assets",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/dev.ts",
    "build": "bunx tsc -p tsconfig.json --noEmit false --outDir dist",
    "test": "bun test src",
    "lint": "bun run ../quality/lint-package.mjs .",
    "typecheck": "bun run ../quality/typecheck-package.mjs ."
  }
}
